#include<iostream>
using namespace std;
int main(){
    /*
    + addition operator
    - subraction operator
    * multiplication operator
    / devide operator
    % modulo operator

    increment & decrement
    x++ pehle use karo fir value +1 se badha do 
    ++x pehle badhao fir x ki value use karo
    x-- pehle use karo fir value of -1 se ghata do
    --x pehle ghatao fir x ki value use karo
    */
    int x = 29;
    cout<<x<<endl;
    x++;
    cout<<x<<endl;
    x--;
    cout<<x<<endl;
    ++x;
    cout<<x<<endl;
    --x;
    cout<<x++<<endl;

    return 0;
}